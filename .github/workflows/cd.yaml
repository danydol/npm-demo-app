name: Deploy
on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment (locked to production)'
        required: true
        default: 'production'
        type: choice
        options:
          - production
